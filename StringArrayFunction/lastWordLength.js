let arrowFunction = (s) => {
    arr=s.split(" ")

    console.log("The length of last word is "+arr[arr.length-1].length)
}

arrowFunction("Last Word Length")
arrowFunction("East or West India is the Best")